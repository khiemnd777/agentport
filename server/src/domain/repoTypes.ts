export interface RepoConfig {
  label: string;
  path: string;
}

export interface Repo {
  key: string;
  label: string;
  path: string;
}

export interface PublicRepo {
  key: string;
  label: string;
}
